
{{# each url}}
    <div class="container">
        <div class="row">
            {{# each this}}
                <div class="col">
                    <img src="{{this}}" alt="image" class="img-thumbnail w-100" >
                </div>
            {{/each}}
        </div>
    </div>
{{/each}}


